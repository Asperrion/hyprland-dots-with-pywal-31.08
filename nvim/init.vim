set encoding=utf8 " смена кодировки на UTF-8

set expandtab " табы = пробелы

set smarttab " умное орпеделение отступов

" Копирование отступов по новой строке
set autoindent
set smartindent

set number " нумерация строк

syntax on " подсветка синтаксиса

set so=30 " во время скролинга курсор будет всегда в середине экрана
